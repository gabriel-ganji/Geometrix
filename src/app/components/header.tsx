import { Geometrix } from "./geometrix"

export const Header = () => {
    return (
        <header className="w-screen h-12 relative top-0 left-0">
            <Geometrix />
        </header>
    )
}